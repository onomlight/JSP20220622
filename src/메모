선언문 <%!%>  -서블릿파일(JAVA파일)의 멤버 구성

스크립틀릿<% %> ->서블릿파일의 Service 함수내에서 코드 저장
- 새로고침때마다 초기화
-절자치향문 적용(반복/분기..)

표현식 <%= %> - 브라우저에 표시

----------------------------------------------
Form 전송 파라미터 받기

Request 내장객체의 getParameter("파라미터명");
-> HTML 내 속성 = 속성값으로 처리
-> 스타일 적용(Javascript)

// JSP 지시자
- 대표적인 예가 @ page 형태의 지시 코드 // 
- page / include / taglib >> 
jsp 공부하는 목적 > 실기문제위해서 include > 한번씩 보이긴 하나 시험에서도 안나옴 // 배제함 

// include


forward vs 리다이렉트 방식으로 나눠짐 